1. 의미 없는 코드 없애기

2. 비효율적인 코드를 없애기 : 반복되는 코드 (가독성을 챙길 필요가 있음)

3. 컴퓨터는 이진법만 알아 들을 수 있다. 0은 거짓 1은 참이다. 
Check = 0;은 거짓 이지만 Check = 1;은 참이다. 
if (Check)라는 조건 문은 Check가 참일때 즉 Check = 1;이라는 조건이다.
반대로는 if (!Check)는 Check가 거짓일때 즉 Check = 0;이라는 조건이다.

4. 연산자를 잘 사용하면 조건문에서 짧게 사용할 수 있다.

5. 쓴 코드를 왜 썼는지 알기 (이 코드가 되는구나 X)

6. 좀더 코드를 깔끔하게 쓰기 위해서 조건문 반복문 등의 끝에 \n를 출력할때 
조건문을 쓰지말고 해당 문법이 끝난뒤에 쓰는 방법이 있다.

7. 포인터안써도 되는 때에는 쓰지 말것

8.포인터로만 할 수 있다면 포인터로만 하고 다중포인터는 최대한 쓰지 말것 쓰게 돼도 이중포인터까지만 쓸것 삼중포인터는 안쓴다고 말해주셨음  

9.디버깅할때 가설을 먼저 세울것 조건문을 통과 했는지 혹은 정보가 없는지 등등 

10. 문법 적인 부분에 더 신경을 쓸 것

11. 변수를 선언할 때 그 변수가 매개변수여야하는지 지역변수로 충분하지는 않은가 살펴볼 것

12. 코딩 할때 코드의 변수 명에 신경을 쓸껏 이유는 변수명이 정리가 안되어 있으면 내가 코드를 찾을때 의미를 몰라서 에러를 잡는데 시간이 많이 들고 고쳐도 다른곳에서 오류가 터진다.
13. 함수 
정리 혹은 두번 코딩하지 않기 위함

14. 코드는 맞는 위치에 넣는다.